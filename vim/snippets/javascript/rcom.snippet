import * as React from 'react';

import getClassName from 'modules/utils/getClassName';

import styles from './index.scss';

export type Props = {
  $2: $3,
  className?: string;
}

export function ${1:ComponentName}(
  {
    $2,
    className,
  }: Props,
) {
  return (
    ${4:<div className={getClassName(styles.componentName, className)}>Stuff</div>}
  );
}

export default $1;
