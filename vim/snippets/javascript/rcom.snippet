import * as React from 'react';

import styles from './index.scss';

type Props = {
  $2: $3,
}

export function ${1:ComponentName}(
  {
    $2,
  }: Props,
) {
  return (
    ${4:<div>Stuff</div>}
  );
}

export default $1;
